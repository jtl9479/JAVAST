하위 클래스가 생성 되는 과정

하위 클래스가 생성되기 전에 상위 클래스가 먼저 생성된다.

확인 하는 방법

하위클래스, 상위클래스 생성자 내부에 print문을 선언
하위클래스를 실행할 경우
상위클래스의 print문이 하위클래스보다 먼저 호출된다.

하위클래스 생성자 내부에는 상위클래스 생성자가 존재한다. (super() )
 
 
형 변환(업 캐스팅)
	상위 클래스로 변수를 선언하고 하위 클래스의 생성자로 인스턴스를 생성
		Customer vc = new VIPCustomer();

		VIPCustomer() 생성자에 의해 VIPCustomer클래스의 모든 멤버 변수에 대한 메모리는 생성되었지만,
		변수의 타입이 Customer이므로 실제 접근 가능한 변수나 메소드는 Customer의 변수와 메소드 이다.
		-> 생성자의 값은 하위클래스의 값을 사용할수 있으나, 변수또는 메소드는 상위클래스의 것만을 사용할수 있다.	
	
	상위 클래스 타입의 변수에 하위 클래스 변수가 대입
	 	VIPCustomer vCustomer = new VIPCustomer();
	 	addCustomer(vCustomer);
	 	int addCustomer(Customer customer){
	 
	 	}
	하위 클래스는 상위 클래스의 타입을 내포하고 있으므로 상위 클래스로의 묵시적 형변환이 가능함
	상속 관계에서 모든 하위 클래스는 상위 클래스로 형 변환(업캐스팅)이 됨
		-> 그 역은 성립하지 않음
		
	값은 하위 클래스의 값이 출력되지만 메소드,변수는 상위 클래스의 것만 사용이 가능하다.
	
		
		
	
